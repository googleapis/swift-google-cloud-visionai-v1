// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// Data schema details indicates the data type and the data struct corresponding
/// to the key of user specified annotation.
public struct DataSchemaDetails: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// Type of the annotation.
  public var type: DataSchemaDetails.DataType? = nil

  /// Config for protobuf any type.
  public var protoAnyConfig: DataSchemaDetails.ProtoAnyConfig? = nil

  /// Config for List data type.
  public var listConfig: GoogleCloudWKT.Recursive<DataSchemaDetails.ListConfig>? = nil

  /// Config for CustomizedStruct data type.
  public var customizedStructConfig:
    GoogleCloudWKT.Recursive<DataSchemaDetails.CustomizedStructConfig>? = nil

  /// The granularity associated with this DataSchema.
  public var granularity: DataSchemaDetails.Granularity? = nil

  /// The search strategy to be applied on the `key` above.
  public var searchStrategy: DataSchemaDetails.SearchStrategy? = nil

  /// Initialize a new instance of `DataSchemaDetails`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = DataSchemaDetails().with { $0.type = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// The configuration for `PROTO_ANY` data type.
  public struct ProtoAnyConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The type URI of the proto message.
    public var typeUri: Swift.String = Swift.String()

    /// Initialize a new instance of `ProtoAnyConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ProtoAnyConfig().with { $0.typeUri = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.visionai.v1.DataSchemaDetails.ProtoAnyConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The configuration for `LIST` data type.
  public struct ListConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The value's data schema in the list.
    public var valueSchema: GoogleCloudWKT.Recursive<DataSchemaDetails>? = nil

    /// Initialize a new instance of `ListConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = ListConfig().with { $0.valueSchema = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.visionai.v1.DataSchemaDetails.ListConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The configuration for `CUSTOMIZED_STRUCT` data type.
  public struct CustomizedStructConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Direct child elements data schemas.
    public var fieldSchemas: [Swift.String: DataSchemaDetails] = [:]

    /// Initialize a new instance of `CustomizedStructConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = CustomizedStructConfig().with { $0.fieldSchemas = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.visionai.v1.DataSchemaDetails.CustomizedStructConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// The search strategy for annotations value of the `key`.
  public struct SearchStrategy: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The type of search strategy to be applied on the `key` above.
    /// The allowed `search_strategy_type` is different for different data types,
    /// which is documented in the DataSchemaDetails.DataType. Specifying
    /// unsupported `search_strategy_type` for data types will result in
    /// INVALID_ARGUMENT error.
    public var searchStrategyType: DataSchemaDetails.SearchStrategy.SearchStrategyType? = nil

    /// Optional. Configs the path to the confidence score, and the threshold.
    /// Only if the score is greater than the threshold, current field will be
    /// built into the index. Only applies to leaf nodes using EXACT_SEARCH or
    /// SMART_SEARCH.
    public var confidenceScoreIndexConfig:
      DataSchemaDetails.SearchStrategy.ConfidenceScoreIndexConfig? = nil

    /// Initialize a new instance of `SearchStrategy`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = SearchStrategy().with { $0.searchStrategyType = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Filter on the confidence score. Only adds to index if the confidence
    /// score is higher than the threshold.
    /// Example data schema:
    /// key: "name-confidence-pair"
    /// type: CUSTOMIZED_STRUCT
    /// granularity: GRANULARITY_PARTITION_LEVEL
    /// customized_struct_config {
    ///   field_schemas {
    ///     key: "name"
    ///     type: STRING
    ///     granularity: GRANULARITY_PARTITION_LEVEL
    ///     search_strategy {
    ///       search_strategy_type: SMART_SEARCH
    ///       confidence_score_index_config {
    ///         field_path: "name-confidence-pair.score"
    ///         threshold: 0.6
    ///       }
    ///     }
    ///   }
    ///   field_schemas {
    ///     key: "score"
    ///     type: FLOAT
    ///     granularity: GRANULARITY_PARTITION_LEVEL
    ///   }
    /// }
    /// This means only "name" with score > 0.6 will be indexed.
    public struct ConfidenceScoreIndexConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
      Sendable
    {
      /// Required. The path to the confidence score field. It is a string that
      /// concatenates all the data schema keys along the path. See the example
      /// above. If the data schema contains LIST, use '_ENTRIES' to concatenate.
      /// Example data schema contains a list:
      /// "key": "list-name-score",
      /// "schemaDetails": {
      ///   "type": "LIST",
      ///   "granularity": "GRANULARITY_PARTITION_LEVEL",
      ///   "listConfig": {
      ///     "valueSchema": {
      ///       "type": "CUSTOMIZED_STRUCT",
      ///       "granularity": "GRANULARITY_PARTITION_LEVEL",
      ///       "customizedStructConfig": {
      ///         "fieldSchemas": {
      ///           "name": {
      ///             "type": "STRING",
      ///             "granularity": "GRANULARITY_PARTITION_LEVEL",
      ///             "searchStrategy": {
      ///               "searchStrategyType": "SMART_SEARCH"
      ///               "confidence_score_index_config": {
      ///                 "field_path": "list-name-score._ENTRIES.score",
      ///                 "threshold": "0.9",
      ///               }
      ///             }
      ///           },
      ///           "score": {
      ///             "type": "FLOAT",
      ///             "granularity": "GRANULARITY_PARTITION_LEVEL",
      ///           }
      ///         }
      ///       }
      ///     }
      ///   }
      /// }
      public var fieldPath: Swift.String = Swift.String()

      /// Required. The threshold.
      public var threshold: Swift.Float = Swift.Float()

      /// Initialize a new instance of `ConfidenceScoreIndexConfig`.
      public init() {}

      /// Use `config` to return a new instance of this object, with some fields updated.
      ///
      /// Commonly used to initialize the value, for example:
      ///
      /// ```
      /// let value = ConfidenceScoreIndexConfig().with { $0.fieldPath = ... }
      /// ```
      public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
        var copy = self
        try config(&copy)
        return copy
      }

      public static var _anyTypeUrl: Swift.String {
        return
          "type.googleapis.com/google.cloud.visionai.v1.DataSchemaDetails.SearchStrategy.ConfidenceScoreIndexConfig"
      }
      public init(fromAny any: GoogleCloudWKT.`Any`) throws {
        self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
      }
      public func _pack() throws -> GoogleCloudWKT.Struct {
        return try GoogleCloudWKT._slowAnySerialize(message: self)
      }
    }

    /// The types of search strategies to be applied on the annotation key.
    public enum SearchStrategyType: Codable, Equatable, Sendable {
      /// Annotatation values of the `key` above will not be searchable.
      case noSearch
      /// When searching with `key`, the value must be exactly as the annotation
      /// value that has been ingested.
      case exactSearch
      /// When searching with `key`, Warehouse will perform broad search based on
      /// semantic of the annotation value.
      case smartSearch
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .noSearch
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .noSearch: return 0
        case .exactSearch: return 1
        case .smartSearch: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .noSearch: return "NO_SEARCH"
        case .exactSearch: return "EXACT_SEARCH"
        case .smartSearch: return "SMART_SEARCH"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SearchStrategyType/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "NO_SEARCH": self = .noSearch
        case "EXACT_SEARCH": self = .exactSearch
        case "SMART_SEARCH": self = .smartSearch
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SearchStrategyType/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .noSearch
        case 1: self = .exactSearch
        case 2: self = .smartSearch
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .noSearch: return try container.encode(0)
        case .exactSearch: return try container.encode(1)
        case .smartSearch: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.visionai.v1.DataSchemaDetails.SearchStrategy"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Data type of the annotation.
  public enum DataType: Codable, Equatable, Sendable {
    /// Unspecified type.
    case unspecified
    /// Integer type.
    /// Allowed search strategies:
    /// - DataSchema.SearchStrategy.NO_SEARCH,
    /// - DataSchema.SearchStrategy.EXACT_SEARCH.
    ///   Supports query by IntRangeArray.
    case integer
    /// Float type.
    /// Allowed search strategies:
    /// - DataSchema.SearchStrategy.NO_SEARCH,
    /// - DataSchema.SearchStrategy.EXACT_SEARCH.
    ///   Supports query by FloatRangeArray.
    case float
    /// String type.
    /// Allowed search strategies:
    /// - DataSchema.SearchStrategy.NO_SEARCH,
    /// - DataSchema.SearchStrategy.EXACT_SEARCH,
    /// - DataSchema.SearchStrategy.SMART_SEARCH.
    case string
    /// Supported formats:
    /// %Y-%m-%dT%H:%M:%E*S%E*z (absl::RFC3339_full)
    /// %Y-%m-%dT%H:%M:%E*S
    /// %Y-%m-%dT%H:%M%E*z
    /// %Y-%m-%dT%H:%M
    /// %Y-%m-%dT%H%E*z
    /// %Y-%m-%dT%H
    /// %Y-%m-%d%E*z
    /// %Y-%m-%d
    /// %Y-%m
    /// %Y
    /// Allowed search strategies:
    /// - DataSchema.SearchStrategy.NO_SEARCH,
    /// - DataSchema.SearchStrategy.EXACT_SEARCH.
    ///   Supports query by DateTimeRangeArray.
    case datetime
    /// Struct type.
    /// - SearchStrategy:
    ///   * Data Schema that's CUSTOMIZED_STRUCT cannot set search strategy.
    ///   * Leaf-node elements allow setting search strategy based on element's
    ///     SearchStrategy restriction.
    /// - Nested layer restrictions:
    ///   * Data Schema that's CUSTOMIZED_STRUCT allows its fields to be of
    ///     CUSTOMIZED_STRUCT as well, but the overall layers restriction is 10.
    case customizedStruct
    /// Geo coordinate type.
    /// Allowed search strategies:
    /// - DataSchema.SearchStrategy.NO_SEARCH,
    /// - DataSchema.SearchStrategy.EXACT_SEARCH.
    ///   Supports query by GeoLocationArray.
    case geoCoordinate
    /// Type to pass any proto as available in annotations.proto. Only use
    /// internally.
    /// Available proto types and its corresponding search behavior:
    /// - ImageObjectDetectionPredictionResult, allows SMART_SEARCH on
    ///   display_names and NO_SEARCH.
    /// - ClassificationPredictionResult, allows SMART_SEARCH on display_names
    ///   and NO_SEARCH.
    /// - ImageSegmentationPredictionResult, allows NO_SEARCH.
    /// - VideoActionRecognitionPredictionResult, allows SMART_SEARCH on
    ///   display_name and NO_SEARCH.
    /// - VideoObjectTrackingPredictionResult, allows SMART_SEARCH on
    ///   display_name and NO_SEARCH.
    /// - VideoClassificationPredictionResult, allows SMART_SEARCH on
    ///   display_name and NO_SEARCH.
    /// - OccupancyCountingPredictionResult, allows EXACT_SEARCH on
    ///   stats.full_frame_count.count and NO_SEARCH.
    /// - ObjectDetectionPredictionResult, allows SMART_SEARCH on
    ///   identified_boxes.entity.label_string and NO_SEARCH.
    case protoAny
    /// Boolean type.
    /// Allowed search strategies:
    /// - DataSchema.SearchStrategy.NO_SEARCH,
    /// - DataSchema.SearchStrategy.EXACT_SEARCH.
    case boolean
    /// List type.
    ///  - Each element in the list must be of the exact same data schema;
    ///    otherwise, they are invalid arguments.
    ///  - List level cannot set search strategy. Leaf node level can do.
    ///  - Elements cannot be another list (no list of list).
    ///  - Elements can be CUSTOMIZED_STRUCT, and max number of layers is 10.
    case list
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .integer: return 1
      case .float: return 2
      case .string: return 3
      case .datetime: return 5
      case .customizedStruct: return 6
      case .geoCoordinate: return 7
      case .protoAny: return 8
      case .boolean: return 9
      case .list: return 10
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "DATA_TYPE_UNSPECIFIED"
      case .integer: return "INTEGER"
      case .float: return "FLOAT"
      case .string: return "STRING"
      case .datetime: return "DATETIME"
      case .customizedStruct: return "CUSTOMIZED_STRUCT"
      case .geoCoordinate: return "GEO_COORDINATE"
      case .protoAny: return "PROTO_ANY"
      case .boolean: return "BOOLEAN"
      case .list: return "LIST"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:DataType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "DATA_TYPE_UNSPECIFIED": self = .unspecified
      case "INTEGER": self = .integer
      case "FLOAT": self = .float
      case "STRING": self = .string
      case "DATETIME": self = .datetime
      case "GEO_COORDINATE": self = .geoCoordinate
      case "PROTO_ANY": self = .protoAny
      case "BOOLEAN": self = .boolean
      case "LIST": self = .list
      case "CUSTOMIZED_STRUCT": self = .customizedStruct
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:DataType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .integer
      case 2: self = .float
      case 3: self = .string
      case 5: self = .datetime
      case 6: self = .customizedStruct
      case 7: self = .geoCoordinate
      case 8: self = .protoAny
      case 9: self = .boolean
      case 10: self = .list
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .integer: return try container.encode(1)
      case .float: return try container.encode(2)
      case .string: return try container.encode(3)
      case .datetime: return try container.encode(5)
      case .customizedStruct: return try container.encode(6)
      case .geoCoordinate: return try container.encode(7)
      case .protoAny: return try container.encode(8)
      case .boolean: return try container.encode(9)
      case .list: return try container.encode(10)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The granularity of annotations under this DataSchema.
  public enum Granularity: Codable, Equatable, Sendable {
    /// Unspecified granularity.
    case unspecified
    /// Asset-level granularity (annotations must not contain partition info).
    case assetLevel
    /// Partition-level granularity (annotations must contain partition info).
    case partitionLevel
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .assetLevel: return 1
      case .partitionLevel: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "GRANULARITY_UNSPECIFIED"
      case .assetLevel: return "GRANULARITY_ASSET_LEVEL"
      case .partitionLevel: return "GRANULARITY_PARTITION_LEVEL"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Granularity/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "GRANULARITY_UNSPECIFIED": self = .unspecified
      case "GRANULARITY_ASSET_LEVEL": self = .assetLevel
      case "GRANULARITY_PARTITION_LEVEL": self = .partitionLevel
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Granularity/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .assetLevel
      case 2: self = .partitionLevel
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .assetLevel: return try container.encode(1)
      case .partitionLevel: return try container.encode(2)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.visionai.v1.DataSchemaDetails"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
