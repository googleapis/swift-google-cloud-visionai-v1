// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) public import GoogleWKT

/// Message describing Processor object.
/// Next ID: 19
public struct Processor: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// name of resource.
  public var name: Swift.String = Swift.String()

  /// Output only. [Output only] Create timestamp.
  public var createTime: GoogleWKT.Timestamp? = nil

  /// Output only. [Output only] Update timestamp.
  public var updateTime: GoogleWKT.Timestamp? = nil

  /// Labels as key value pairs.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Required. A user friendly display name for the processor.
  public var displayName: Swift.String = Swift.String()

  /// Illustrative sentences for describing the functionality of the processor.
  public var description: Swift.String = Swift.String()

  /// Output only. Processor Type.
  public var processorType: Processor.ProcessorType = Processor.ProcessorType()

  /// Model Type.
  public var modelType: ModelType = ModelType()

  /// Source info for customer created processor.
  public var customProcessorSourceInfo: CustomProcessorSourceInfo? = nil

  /// Output only. State of the Processor.
  public var state: Processor.ProcessorState = Processor.ProcessorState()

  /// Output only. [Output only] The input / output specifications of a
  /// processor, each type of processor has fixed input / output specs which
  /// cannot be altered by customer.
  public var processorIoSpec: ProcessorIOSpec? = nil

  /// Output only. The corresponding configuration can be used in the Application
  /// to customize the behavior of the processor.
  public var configurationTypeurl: Swift.String = Swift.String()

  public var supportedAnnotationTypes: [StreamAnnotationType] = []

  /// Indicates if the processor supports post processing.
  public var supportsPostProcessing: Swift.Bool = Swift.Bool()

  /// Which instance types this processor supports; if empty, this default to
  /// STREAMING_PREDICTION.
  public var supportedInstanceTypes: [Instance.InstanceType] = []

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `Processor`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Processor().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let name = CodingKeys(stringValue: "name")
    static let createTime = CodingKeys(stringValue: "createTime")
    static let updateTime = CodingKeys(stringValue: "updateTime")
    static let labels = CodingKeys(stringValue: "labels")
    static let displayName = CodingKeys(stringValue: "displayName")
    static let description = CodingKeys(stringValue: "description")
    static let processorType = CodingKeys(stringValue: "processorType")
    static let modelType = CodingKeys(stringValue: "modelType")
    static let customProcessorSourceInfo = CodingKeys(stringValue: "customProcessorSourceInfo")
    static let state = CodingKeys(stringValue: "state")
    static let processorIoSpec = CodingKeys(stringValue: "processorIoSpec")
    static let configurationTypeurl = CodingKeys(stringValue: "configurationTypeurl")
    static let supportedAnnotationTypes = CodingKeys(stringValue: "supportedAnnotationTypes")
    static let supportsPostProcessing = CodingKeys(stringValue: "supportsPostProcessing")
    static let supportedInstanceTypes = CodingKeys(stringValue: "supportedInstanceTypes")

    static let _knownKeys: Set<Swift.String> = [
      "name",
      "createTime",
      "updateTime",
      "labels",
      "displayName",
      "description",
      "processorType",
      "modelType",
      "customProcessorSourceInfo",
      "state",
      "processorIoSpec",
      "configurationTypeurl",
      "supportedAnnotationTypes",
      "supportsPostProcessing",
      "supportedInstanceTypes",
    ]
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
      self.name = value
    }
    self.createTime = try container.decodeIfPresent(GoogleWKT.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(GoogleWKT.Timestamp.self, forKey: .updateTime)
    if let value = try container.decodeIfPresent([Swift.String: Swift.String].self, forKey: .labels)
    {
      self.labels = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .displayName) {
      self.displayName = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .description) {
      self.description = value
    }
    if let value = try container.decodeIfPresent(
      Processor.ProcessorType.self, forKey: .processorType)
    {
      self.processorType = value
    }
    if let value = try container.decodeIfPresent(ModelType.self, forKey: .modelType) {
      self.modelType = value
    }
    self.customProcessorSourceInfo = try container.decodeIfPresent(
      CustomProcessorSourceInfo.self, forKey: .customProcessorSourceInfo)
    if let value = try container.decodeIfPresent(Processor.ProcessorState.self, forKey: .state) {
      self.state = value
    }
    self.processorIoSpec = try container.decodeIfPresent(
      ProcessorIOSpec.self, forKey: .processorIoSpec)
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .configurationTypeurl) {
      self.configurationTypeurl = value
    }
    if let value = try container.decodeIfPresent(
      [StreamAnnotationType].self, forKey: .supportedAnnotationTypes)
    {
      self.supportedAnnotationTypes = value
    }
    if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .supportsPostProcessing) {
      self.supportsPostProcessing = value
    }
    if let value = try container.decodeIfPresent(
      [Instance.InstanceType].self, forKey: .supportedInstanceTypes)
    {
      self.supportedInstanceTypes = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encodeIfPresent(self.createTime, forKey: .createTime)
    try container.encodeIfPresent(self.updateTime, forKey: .updateTime)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.displayName, forKey: .displayName)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.processorType, forKey: .processorType)
    try container.encode(self.modelType, forKey: .modelType)
    try container.encodeIfPresent(
      self.customProcessorSourceInfo, forKey: .customProcessorSourceInfo)
    try container.encode(self.state, forKey: .state)
    try container.encodeIfPresent(self.processorIoSpec, forKey: .processorIoSpec)
    try container.encode(self.configurationTypeurl, forKey: .configurationTypeurl)
    try container.encode(self.supportedAnnotationTypes, forKey: .supportedAnnotationTypes)
    try container.encode(self.supportsPostProcessing, forKey: .supportsPostProcessing)
    try container.encode(self.supportedInstanceTypes, forKey: .supportedInstanceTypes)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Type
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum ProcessorType: Codable, Equatable, Sendable {
    /// Processor Type UNSPECIFIED.
    case unspecified
    /// Processor Type PRETRAINED.
    /// Pretrained processor is developed by Vision AI App Platform with
    /// state-of-the-art vision data processing functionality, like occupancy
    /// counting or person blur. Pretrained processor is usually publicly
    /// available.
    case pretrained
    /// Processor Type CUSTOM.
    /// Custom processors are specialized processors which are either uploaded by
    /// customers or imported from other GCP platform (for example Vertex AI).
    /// Custom processor is only visible to the creator.
    case custom
    /// Processor Type CONNECTOR.
    /// Connector processors are special processors which perform I/O for the
    /// application, they do not processing the data but either deliver the data
    /// to other processors or receive data from other processors.
    case connector
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .pretrained: return 1
      case .custom: return 2
      case .connector: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PROCESSOR_TYPE_UNSPECIFIED"
      case .pretrained: return "PRETRAINED"
      case .custom: return "CUSTOM"
      case .connector: return "CONNECTOR"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ProcessorType/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PROCESSOR_TYPE_UNSPECIFIED": self = .unspecified
      case "PRETRAINED": self = .pretrained
      case "CUSTOM": self = .custom
      case "CONNECTOR": self = .connector
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ProcessorType/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .pretrained
      case 2: self = .custom
      case 3: self = .connector
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("PROCESSOR_TYPE_UNSPECIFIED")
      case .pretrained: return try container.encode("PRETRAINED")
      case .custom: return try container.encode("CUSTOM")
      case .connector: return try container.encode("CONNECTOR")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum ProcessorState: Codable, Equatable, Sendable {
    /// Unspecified Processor state.
    case unspecified
    /// Processor is being created (not ready for use).
    case creating
    /// Processor is and ready for use.
    case active
    /// Processor is being deleted (not ready for use).
    case deleting
    /// Processor deleted or creation failed .
    case failed
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .creating: return 1
      case .active: return 2
      case .deleting: return 3
      case .failed: return 4
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "PROCESSOR_STATE_UNSPECIFIED"
      case .creating: return "CREATING"
      case .active: return "ACTIVE"
      case .deleting: return "DELETING"
      case .failed: return "FAILED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:ProcessorState/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "PROCESSOR_STATE_UNSPECIFIED": self = .unspecified
      case "CREATING": self = .creating
      case "ACTIVE": self = .active
      case "DELETING": self = .deleting
      case "FAILED": self = .failed
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:ProcessorState/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .creating
      case 2: self = .active
      case 3: self = .deleting
      case 4: self = .failed
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("PROCESSOR_STATE_UNSPECIFIED")
      case .creating: return try container.encode("CREATING")
      case .active: return try container.encode("ACTIVE")
      case .deleting: return try container.encode("DELETING")
      case .failed: return try container.encode("FAILED")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.visionai.v1.Processor"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
