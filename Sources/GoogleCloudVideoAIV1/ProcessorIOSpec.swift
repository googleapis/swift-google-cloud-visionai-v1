// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// Message describing the input / output specifications of a processor.
public struct ProcessorIOSpec: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// For processors with input_channel_specs, the processor must be explicitly
  /// connected to another processor.
  public var graphInputChannelSpecs: [ProcessorIOSpec.GraphInputChannelSpec] = []

  /// The output artifact specifications for the current processor.
  public var graphOutputChannelSpecs: [ProcessorIOSpec.GraphOutputChannelSpec] = []

  /// The input resource that needs to be fed from the application instance.
  public var instanceResourceInputBindingSpecs: [ProcessorIOSpec.InstanceResourceInputBindingSpec] =
    []

  /// The output resource that the processor will generate per instance.
  /// Other than the explicitly listed output bindings here, all the processors'
  /// GraphOutputChannels can be binded to stream resource. The bind name then is
  /// the same as the GraphOutputChannel's name.
  public var instanceResourceOutputBindingSpecs:
    [ProcessorIOSpec.InstanceResourceOutputBindingSpec] = []

  /// Initialize a new instance of `ProcessorIOSpec`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ProcessorIOSpec().with { $0.graphInputChannelSpecs = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Message for input channel specification.
  public struct GraphInputChannelSpec: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The name of the current input channel.
    public var name: Swift.String = Swift.String()

    /// The data types of the current input channel.
    /// When this field has more than 1 value, it means this input channel can be
    /// connected to either of these different data types.
    public var dataType: DataType = DataType()

    /// If specified, only those detailed data types can be connected to the
    /// processor. For example, jpeg stream for MEDIA, or PredictionResult proto
    /// for PROTO type. If unspecified, then any proto is accepted.
    public var acceptedDataTypeUris: [Swift.String] = []

    /// Whether the current input channel is required by the processor.
    /// For example, for a processor with required video input and optional audio
    /// input, if video input is missing, the application will be rejected while
    /// the audio input can be missing as long as the video input exists.
    public var `required`: Swift.Bool = Swift.Bool()

    /// How many input edges can be connected to this input channel. 0 means
    /// unlimited.
    public var maxConnectionAllowed: Swift.Int64 = Swift.Int64()

    /// Initialize a new instance of `GraphInputChannelSpec`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GraphInputChannelSpec().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case name = "name"
      case dataType = "dataType"
      case acceptedDataTypeUris = "acceptedDataTypeUris"
      case `required` = "required"
      case maxConnectionAllowed = "maxConnectionAllowed"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.name = try container.decode(Swift.String.self, forKey: .name)
      self.dataType = try container.decode(DataType.self, forKey: .dataType)
      self.acceptedDataTypeUris = try container.decode(
        [Swift.String].self, forKey: .acceptedDataTypeUris)
      self.`required` = try container.decode(Swift.Bool.self, forKey: .`required`)
      self.maxConnectionAllowed = try container.decode(
        Swift.Int64.self, forKey: .maxConnectionAllowed)
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.name, forKey: .name)
      try container.encode(self.dataType, forKey: .dataType)
      try container.encode(self.acceptedDataTypeUris, forKey: .acceptedDataTypeUris)
      try container.encode(self.`required`, forKey: .`required`)
      try container.encode(self.maxConnectionAllowed, forKey: .maxConnectionAllowed)
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.visionai.v1.ProcessorIOSpec.GraphInputChannelSpec"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Message for output channel specification.
  public struct GraphOutputChannelSpec: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// The name of the current output channel.
    public var name: Swift.String = Swift.String()

    /// The data type of the current output channel.
    public var dataType: DataType = DataType()

    public var dataTypeUri: Swift.String = Swift.String()

    /// Initialize a new instance of `GraphOutputChannelSpec`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = GraphOutputChannelSpec().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.cloud.visionai.v1.ProcessorIOSpec.GraphOutputChannelSpec"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  /// Message for instance resource channel specification.
  /// External resources are virtual nodes which are not expressed in the
  /// application graph. Each processor expresses its out-graph spec, so customer
  /// is able to override the external source or destinations to the
  public struct InstanceResourceInputBindingSpec: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Name of the input binding, unique within the processor.
    public var name: Swift.String = Swift.String()

    public var resourceType: OneOf_ResourceType? = nil

    /// Initialize a new instance of `InstanceResourceInputBindingSpec`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = InstanceResourceInputBindingSpec().with { $0.configTypeUri = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private enum CodingKeys: Swift.String, CodingKey {
      case configTypeUri = "configTypeUri"
      case resourceTypeUri = "resourceTypeUri"
      case name = "name"
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      self.name = try container.decode(Swift.String.self, forKey: .name)

      var resourceType: OneOf_ResourceType? = nil
      let resourceTypeCheckAndSet = {
        if resourceType != nil {
          throw DecodingError.dataCorrupted(
            DecodingError.Context(
              codingPath: decoder.codingPath,
              debugDescription: "Multiple values set for oneof 'resourceType'"))
        }
        resourceType = $0
      }
      if let configTypeUri = try container.decodeIfPresent(
        Swift.String.self, forKey: .configTypeUri)
      {
        try resourceTypeCheckAndSet(.configTypeUri(configTypeUri))
      }
      if let resourceTypeUri = try container.decodeIfPresent(
        Swift.String.self, forKey: .resourceTypeUri)
      {
        try resourceTypeCheckAndSet(.resourceTypeUri(resourceTypeUri))
      }
      self.resourceType = resourceType
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.name, forKey: .name)

      if let choice = self.resourceType {
        switch choice {
        case .configTypeUri(let value):
          try container.encode(value, forKey: .configTypeUri)
        case .resourceTypeUri(let value):
          try container.encode(value, forKey: .resourceTypeUri)
        }
      }
    }

    public enum OneOf_ResourceType: Codable, Equatable, Sendable {
      /// The configuration proto that includes the Googleapis resources. I.e.
      /// type.googleapis.com/google.cloud.vision.v1.StreamWithAnnotation
      case configTypeUri(Swift.String)
      /// The direct type url of Googleapis resource. i.e.
      /// type.googleapis.com/google.cloud.vision.v1.Asset
      case resourceTypeUri(Swift.String)
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.visionai.v1.ProcessorIOSpec.InstanceResourceInputBindingSpec"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  public struct InstanceResourceOutputBindingSpec: Codable, Equatable, GoogleCloudWkt._AnyPackable,
    Sendable
  {
    /// Name of the output binding, unique within the processor.
    public var name: Swift.String = Swift.String()

    /// The resource type uri of the acceptable output resource.
    public var resourceTypeUri: Swift.String = Swift.String()

    /// Whether the output resource needs to be explicitly set in the instance.
    /// If it is false, the processor will automatically generate it if required.
    public var explicit: Swift.Bool = Swift.Bool()

    /// Initialize a new instance of `InstanceResourceOutputBindingSpec`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = InstanceResourceOutputBindingSpec().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.cloud.visionai.v1.ProcessorIOSpec.InstanceResourceOutputBindingSpec"
    }
    public init(fromAny any: GoogleCloudWkt.`Any`) throws {
      self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWkt.Struct {
      return try GoogleCloudWkt._slowAnySerialize(message: self)
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.visionai.v1.ProcessorIOSpec"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
