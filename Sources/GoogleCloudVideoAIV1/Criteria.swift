// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWkt

/// Filter criteria applied to current search results.
public struct Criteria: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// The UGA field or ML field to apply filtering criteria.
  public var field: Swift.String = Swift.String()

  /// If true, return query matched annotations for this criteria.
  /// This option is only applicable for inclusion criteria, i.e., not exclusion
  /// criteria, with partition level annotations.  It supports the following data
  /// types:
  ///  - INTEGER
  ///  - FLOAT
  ///  - STRING (DataSchema.SearchStrategy.EXACT_SEARCH only)
  ///  - BOOLEAN
  public var fetchMatchedAnnotations: Swift.Bool = Swift.Bool()

  public var value: OneOf_Value? = nil

  /// Initialize a new instance of `Criteria`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Criteria().with { $0.textArray = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case textArray = "textArray"
    case intRangeArray = "intRangeArray"
    case floatRangeArray = "floatRangeArray"
    case dateTimeRangeArray = "dateTimeRangeArray"
    case geoLocationArray = "geoLocationArray"
    case boolValue = "boolValue"
    case field = "field"
    case fetchMatchedAnnotations = "fetchMatchedAnnotations"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.field = try container.decode(Swift.String.self, forKey: .field)
    self.fetchMatchedAnnotations = try container.decode(
      Swift.Bool.self, forKey: .fetchMatchedAnnotations)

    var value: OneOf_Value? = nil
    let valueCheckAndSet = {
      if value != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'value'"))
      }
      value = $0
    }
    if let textArray = try container.decodeIfPresent(StringArray?.self, forKey: .textArray) {
      try valueCheckAndSet(.textArray(textArray))
    }
    if let intRangeArray = try container.decodeIfPresent(
      IntRangeArray?.self, forKey: .intRangeArray)
    {
      try valueCheckAndSet(.intRangeArray(intRangeArray))
    }
    if let floatRangeArray = try container.decodeIfPresent(
      FloatRangeArray?.self, forKey: .floatRangeArray)
    {
      try valueCheckAndSet(.floatRangeArray(floatRangeArray))
    }
    if let dateTimeRangeArray = try container.decodeIfPresent(
      DateTimeRangeArray?.self, forKey: .dateTimeRangeArray)
    {
      try valueCheckAndSet(.dateTimeRangeArray(dateTimeRangeArray))
    }
    if let geoLocationArray = try container.decodeIfPresent(
      GeoLocationArray?.self, forKey: .geoLocationArray)
    {
      try valueCheckAndSet(.geoLocationArray(geoLocationArray))
    }
    if let boolValue = try container.decodeIfPresent(BoolValue?.self, forKey: .boolValue) {
      try valueCheckAndSet(.boolValue(boolValue))
    }
    self.value = value
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.field, forKey: .field)
    try container.encode(self.fetchMatchedAnnotations, forKey: .fetchMatchedAnnotations)

    if let choice = self.value {
      switch choice {
      case .textArray(let value):
        try container.encode(value, forKey: .textArray)
      case .intRangeArray(let value):
        try container.encode(value, forKey: .intRangeArray)
      case .floatRangeArray(let value):
        try container.encode(value, forKey: .floatRangeArray)
      case .dateTimeRangeArray(let value):
        try container.encode(value, forKey: .dateTimeRangeArray)
      case .geoLocationArray(let value):
        try container.encode(value, forKey: .geoLocationArray)
      case .boolValue(let value):
        try container.encode(value, forKey: .boolValue)
      }
    }
  }

  public enum OneOf_Value: Codable, Equatable, Sendable {
    /// The text values associated with the field.
    indirect case textArray(StringArray?)
    /// The integer ranges associated with the field.
    indirect case intRangeArray(IntRangeArray?)
    /// The float ranges associated with the field.
    indirect case floatRangeArray(FloatRangeArray?)
    /// The datetime ranges associated with the field.
    indirect case dateTimeRangeArray(DateTimeRangeArray?)
    /// Geo Location array.
    indirect case geoLocationArray(GeoLocationArray?)
    /// A Boolean value.
    indirect case boolValue(BoolValue?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.cloud.visionai.v1.Criteria"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
